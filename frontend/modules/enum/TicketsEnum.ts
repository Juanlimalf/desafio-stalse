export enum TicketChannel {
  Whatsapp = "whatsapp",
  Email = "email",
  Chat = "chat",
  Telefone = "telefone",
  Instagram = "instagram",
}

export enum TicketStatus {
  Opened = "opened",
  Closed = "closed",
}

export enum TicketPriority {
  Low = "low",
  Medium = "medium",
  High = "high",
}
